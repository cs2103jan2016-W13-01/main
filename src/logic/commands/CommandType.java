/* @@author A0112184R;
 * enum CommandType: contains possible types for user commands.
 */
package logic.commands;

public enum CommandType {
	ADD, DELETE, CLEAR, DISPLAY, INVALID, UNDO, MARK, UNMARK, EDIT, SEARCH, SET, HELP, REDO;
}
